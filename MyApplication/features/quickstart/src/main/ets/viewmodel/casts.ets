export class Casts {
  province!: string;
  city!: string;
  adcode!: string; // 修改为 string
  weather!: string;
  temperature!: number;
  winddirection!: string;
  windpower!: string;
  humidity!: number;
  reporttime!: string; // 修改为 string
  temperature_float!: number;
  humidity_float!: number;
}
